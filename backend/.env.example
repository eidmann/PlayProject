# Copy to .env (gitignored) and fill in real values. Never commit .env.
#
# Neon (https://neon.tech):
# - Dev branch: pooled → DATABASE_URL, direct → DIRECT_URL
# - Separate test branch (Vitest wipes data): pooled → TEST_DATABASE_URL, direct → TEST_DIRECT_URL
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/mindlog?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/mindlog?sslmode=require"
TEST_DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/mindlog?sslmode=require"
TEST_DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/mindlog?sslmode=require"
PORT=3001

# Required from milestone 5 (AI features)
# OPENAI_API_KEY=sk-...
